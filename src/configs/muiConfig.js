import { createTheme } from "@mui/material"

const theme = {
    palette: {
        primary: {
            main: "#f95959",
        },
        secondary: {
            main: "#fff",
        },
    },
}

export default createTheme(theme);